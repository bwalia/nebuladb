# nginx config for the showcase. nginx's official image runs
# `envsubst` over every file in /etc/nginx/templates/ on startup,
# replacing `${NEBULA_SERVER}` with the env var. That lets the same
# image work in compose (service name) or standalone (host.docker.internal).
#
# Read failover: GET / HEAD requests go through the `nebula_read_pool`
# upstream so reads stay available during a leader outage (the
# follower serves them). Writes (POST / PUT / PATCH / DELETE) stay
# pinned to ${NEBULA_SERVER} since the follower currently rejects
# them with 409. When the leader is back up, the pool's primary is
# preferred again automatically by nginx's `backup` directive.

# Docker's embedded DNS — needed so we can use a variable in
# proxy_pass (otherwise nginx resolves once at boot and never again).
resolver 127.0.0.11 valid=30s ipv6=off;

# Read pool: leader primary, follower as backup. nginx only sends
# to the backup when the primary is marked failed by its own probes
# (max_fails consecutive errors within fail_timeout). 503 / timeout
# count as errors via the proxy_next_upstream directive below.
upstream nebula_read_pool {
    server nebula-server:8080 max_fails=2 fail_timeout=5s;
    server nebula-follower:8080 backup max_fails=2 fail_timeout=5s;
    keepalive 16;
}

# Route reads through the pool; writes go straight to ${NEBULA_SERVER}.
# `map` is evaluated per request, so a sustained outage on the leader
# correctly steers reads to the follower without nginx reload.
map $request_method $api_upstream {
    default                     "${NEBULA_SERVER}";
    GET                         "http://nebula_read_pool";
    HEAD                        "http://nebula_read_pool";
}

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: anything that isn't a real file resolves to
    # index.html so client-side navigation works on deep links.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API forwarding. Same paths the server exposes, proxied
    # transparently — the browser sees only port 80.
    location /api/ {
        proxy_pass $api_upstream;
        proxy_http_version 1.1;
        # These three headers are what upstream rate-limit / auth
        # middleware uses to identify the caller; forward them so
        # behavior matches a direct connection.
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # Connection: "" lets nginx use keepalive against the upstream.
        proxy_set_header Connection "";
        # Treat connection-refused / timeout / 5xx from the leader as
        # cause to try the next upstream (the follower). Without this
        # nginx returns the failure to the client instead of failing
        # over.
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries 2;
        # Disable buffering so SSE (text/event-stream) from /ai/rag
        # reaches the browser byte-by-byte as the LLM emits tokens.
        # Without this nginx holds onto the response until it hits
        # a buffer threshold and the UI feels stuck.
        proxy_buffering off;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }
    location /healthz {
        proxy_pass http://nebula_read_pool;
        proxy_set_header Host $host;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }
    location /metrics {
        proxy_pass ${NEBULA_SERVER};
        proxy_set_header Host $host;
    }

    # Cache immutable hashed assets aggressively; index.html stays
    # short-cached so a redeploy actually reaches users.
    location ~* \.(js|css|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
